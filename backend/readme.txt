The backend is run on django-rest-framwork

To set up the server:
** pip install -r requirements.txt
** to set up the database
    ** python manage.py makemigrations
    ** python manage.py migrate
** to run the server
    ** python manage.py runserver
